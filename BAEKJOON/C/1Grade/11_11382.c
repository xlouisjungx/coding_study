#include <stdio.h>

int main(void) {
    int num1 = 0, num2 = 0, num3 = 0, sum = 0;

    scanf("%d %d %d", &num1, &num2, &num3);
    sum = num1 + num2 + num3;
    printf("%d\n", sum);

    return 0;
}